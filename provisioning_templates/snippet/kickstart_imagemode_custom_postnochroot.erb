<%#
name: Kickstart imagemode custom postnochroot
description: This adds users from admin_users and runs a finisher script from zfs1
snippet: true
model: ProvisioningTemplate
organizations:
- Gonoph
locations:
- Home
-%>
<%#
  not sure why there is a sysimage and a sysroot, but sysroot is where all the
  good stuff is. There isn't a resolv.conf in /mnt/sysroot/etc due to container.
%>
echo "####################"
echo "##### Copying /etc/resolv.conf into /mnt/sysroot/etc
echo "####################"
cp -va /etc/resolv.conf /mnt/sysroot/etc/resolv.conf
